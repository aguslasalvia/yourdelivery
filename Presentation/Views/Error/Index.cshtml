@model string;

<div class="text-center vh-100 d-flex flex-column justify-content-center align-items-center">
    <i class="fs-1 bi bi-exclamation-octagon-fill text-danger"></i>
    <h1 class="fs-4 fw-bold">@ViewBag.ErrorMessage</h1>
</div>