@using Core.Enums
@using DTO.Users
@model UsersViewModelProfile

<section class="mt-5 d-flex flex-column justify-content-center">
    <div>
        <figure class="d-flex align-content-center justify-content-center">
            @if (Enum.GetName(Model.User.Gender) == Gender.Female.ToString())
            {
                <img src="/assets/womanProfile.png" alt="User Photo">    
            }
            else
            {
                <img src="/assets/manProfile.png" alt="User Photo">
            }
            
        </figure>
        
        @* This is a mode of example, this will change into a grid of inputs*@
        
        <div class="d-flex flex-column justify-content-center align-content-center gap-2  mt-3">
        <div class="input-group d-flex flex-row justify-content-center align-content-center gap-4">
            <input class="mb-0 form-control" value="@Model.User.Name @Model.User.Lastname" />
            <input class="mb-0 form-control" value="@Model.User.Email"/>
        </div>
        <div class=" input-group d-flex flex-row justify-content-center align-content-center gap-4">
            <input class="mb-0 form-control" value="+598 99 266 444"/>
            <input class="mb-0 form-control" value="5.575.575-5"/>

        </div>
        <div class=" input-group d-flex justify-content-center align-content-center gap-4">
            <input type="date" class="mb-0  form-control" value="06/07/1989"/>
            <input type="date" class="mb-0  form-control"  value="1989-06-07"/>
        </div>
        </div>
    </div>
    <div class="d-flex flex-row justify-content-center mt-5 gap-3" >
        <button class="btn btn-primary mb-2" title="Edit Profile"><i class="bi bi-pencil-square"></i></button>
        <br/>
        @if(Context.Session.GetString("Role") == Role.Administrator.ToString()) {
            <button class="btn btn-danger mb-2" title="Delete Profile"><i class="bi bi-trash"></i></button>
        }
    </div>
</section>