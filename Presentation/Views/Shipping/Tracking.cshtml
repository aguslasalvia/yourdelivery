@using Core.Enums
@using Microsoft.EntityFrameworkCore.Metadata.Internal
<div class="d-flex justify-content-center align-content-center flex-column mt-5 ">
    <h1 class="w-100 text-center"> Find your package</h1>

    <form class="w-100 align-content-center justify-content-center d-flex">
        <div class="input-group d-flex justify-content-center w-50 align-content-center">
            <span class="input-group-text" id="basic-addon1"><i class="bi bi-geo-alt-fill"></i></span>
            <input type="email" class="form-control" name="Tracking" placeholder="Tracking Number" aria-label="Tracking"
                   aria-describedby="basic-addon1" required>
        </div>
    </form>

    <figure class="mt-5 w-100 d-flex justify-content-center">
        <img src="/assets/tracking.png" alt="Tracking image"/>
    </figure>
    
    
    @if (ViewBag.Shipping != null)
    {
        <h2 class="text-center">Your Package State</h2>        
        <div class="flex justify-content-center align-content-center text-center">
            <p>Tracking Number: @ViewBag.Shipping</p>
            <p>Client Email: </p>
            <p>Employee Email</p>
            
            @* Change this in the future *@
            @if (ViewBag.State == null)
            {
                <p class="process">Status: @ViewBag.State</p>
            }
            else
            {   
                <p class="finish">Status: @ViewBag.State</p>

            }
            
        </div>
    }




</div>